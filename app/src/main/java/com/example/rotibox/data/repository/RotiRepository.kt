package com.example.rotibox.data.repository

class RotiRepository {
    // This is a placeholder. Later you will pass your DAO here.

    suspend fun getTotalRotiCount(): Int {
        // Mocking data for now so the app compiles
        return 15
    }
}